{
  "name": "tether",
  "displayName": "Tether",
  "description": "Drive a remote Windows machine through Claude Code via a one-shot agent .exe and a cloud relay. Filesystem, shell, registry, event log, screenshots, clipboard, Active Directory, and window control without installing anything beyond the .exe on the remote.",
  "version": "0.2.78",
  "publisher": "WildFrog Systems",
  "homepage": "https://github.com/EntityReborn/tether-plugin",
  "repository": "https://github.com/EntityReborn/tether-plugin",
  "mcpServers": {
    "tether": {
      "command": "${CLAUDE_PLUGIN_DATA}/tether-mcp.exe",
      "args": [],
      "description": "Tether MCP server. Spawns one process per Claude Code window and holds the WSS connection to the relay for that session.",
      "env": {
        "TETHER_DEFAULT_RELAY": "wss://tether.up.railway.app"
      }
    }
  }
}
